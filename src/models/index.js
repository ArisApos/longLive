import { changeProductContainer, onKeyUp } from './actions'
import { searchedProducts, suggestedProducts } from './reducers'
import { products, brands, types, prices } from './data'

export {
  changeProductContainer, onKeyUp,
  products, brands, types, prices,
  searchedProducts, suggestedProducts
}
